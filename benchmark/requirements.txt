# ISON Benchmark Dependencies
# Install with: pip install -r requirements.txt

# Token counting (o200k_base tokenizer for GPT-4o/GPT-5)
tiktoken>=0.5.0

# YAML format support
pyyaml>=6.0

# HTTP requests for LLM API calls
requests>=2.28.0

# Official ISON parser (PyPI: ison-py)
ison-py>=1.0.0

# Official TOON parser (PyPI: toon-llm)
toon-llm>=1.0.0b6
